#include<stdio.h>
int main()
{
    int n,i,sum=0;
    printf("how many value's are input to summation::\n");
    scanf("%d",n);
    for(i=n;i<=n;i++)
    {
        sum=sum+i;
    }
    printf("Enter any vlaue a and b:\n");
       //scanf("%d%d",&a,&b);
      // sum=a+b;
       //printf("Additon=%d\n",sum);

    return 0;
}
